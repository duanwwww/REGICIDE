#include <board.hpp>
class Battle {
private:
    Board *board;

public:
    Battle(std::vector<Character *>); // HAVE NOT FINISHED
    void turn();                      // HAVE NOT FINISHED
    void start_turn();                // HAVE NOT FINISHED
    void play_cards();                // HAVE NOT FINISHED
    void settle_effects();            // HAVE NOT FINISHED
    void deal_damage();               // HAVE NOT FINISHED
    void check_death();               // HAVE NOT FINISHED
    void end_turn();                  // HAVE NOT FINISHED
};

class BattleInfo {
    // to be fullfill
};